# ── HTTP -> HTTPS Redirect ──
server {
    listen 80;
    server_name sarpdag.me www.sarpdag.me;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# ── HTTPS Server ──
server {
    listen 443 ssl;
    server_name sarpdag.me www.sarpdag.me;

    ssl_certificate /etc/letsencrypt/live/sarpdag.me/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sarpdag.me/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Proxy API and WebSocket requests to backend container
    location ~ ^/(auth|users|membership-plans|enrollments|exercises|workout-programs|health-profile|programs|check-ins|dashboard|gyms|messages|uploads|socket.io) {
        proxy_pass http://backend:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
